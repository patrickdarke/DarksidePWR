// Copy to secrets.h (gitignored) and fill in. Never commit real credentials.
//
// Only Wi-Fi credentials are required here — every other setting lives in
// config.h with committed defaults, and the setup screen (gear button)
// handles Wi-Fi/GX/units/brightness at runtime anyway. Any config.h define
// may be overridden from this file if you prefer keeping it out of git.
#pragma once

#define WIFI_SSID "YOUR_TRUCK_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
